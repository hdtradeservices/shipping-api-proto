{
  "swagger": "2.0",
  "info": {
    "title": "Fulfillment Integration API",
    "version": "v2"
  },
  "tags": [
    {
      "name": "Fulfillment Integration Service",
      "description": "Drain two queues: ship what Zentail asks you to ship, and cancel what it asks you to pull back. Inventory has its own contract — see the Inventory Integration API."
    },
    {
      "name": "Warehouse Service",
      "description": "Implemented by the integration. Zentail calls this to ask the integration about its own health."
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v2/shipping/fulfillment_order": {
      "get": {
        "summary": "GetFulfillmentOrder fetches one fulfillment order, whatever queue it is or\nis not in. For troubleshooting and for reconciling after a crash.",
        "description": "Pass exactly one of fulfillment_order_id or external_order_id. This is a\npoint lookup, not a queue: an order it returns may still owe a shipment,\nand only ListNeedToShip says so. Polling it in place of the queues tells\nyou what exists, never what Zentail wants done.",
        "operationId": "FulfillmentIntegrationService_GetFulfillmentOrder",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiFulfillmentOrder"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "fulfillmentOrderId",
            "description": "Zentail's id, as it appears on any fulfillment order you have been given.",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "externalOrderId",
            "description": "The id you recorded with AcknowledgeFulfillmentOrders. Unique within your\nintegration, so it resolves to exactly one fulfillment order — which is\nwhat makes it usable for reconciling after a crash.",
            "in": "query",
            "required": false,
            "type": "string"
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/fulfillment_orders/acknowledge": {
      "post": {
        "summary": "AcknowledgeFulfillmentOrders records the identifier you gave the work on\nyour own side. It does **not** drain ListNeedToShip — you still owe the\nshipment — and it is not required before shipping.",
        "description": "Two reasons to call it. It is how a crash between \"created it on my side\"\nand \"told Zentail\" becomes recoverable: the fulfillment order is still in\nListNeedToShip, and the absence of your id there tells you to reconcile\nbefore creating a duplicate. And it is what puts the work into\nListNeedToCancel if Zentail later needs it pulled back.",
        "operationId": "FulfillmentIntegrationService_AcknowledgeFulfillmentOrders",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiAcknowledgeFulfillmentOrdersResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiAcknowledgeFulfillmentOrdersRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/fulfillment_orders/alerts": {
      "post": {
        "summary": "RaiseAlerts raises alerts against fulfillment orders, using the same alert\nmodel the rest of Zentail already shows on a customer order.",
        "description": "Idempotent by design: Zentail refuses a second open alert of the same type\non the same order, so a poller can raise the same condition every pass\nwithout creating noise. No client-side \"have I already alerted?\"\nbookkeeping.\n\nAn alert is not a substitute for rejecting. Raise one to explain a delay;\nreject when you are not going to ship.",
        "operationId": "FulfillmentIntegrationService_RaiseAlerts",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiRaiseAlertsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiRaiseAlertsRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/fulfillment_orders/alerts/resolve": {
      "post": {
        "summary": "ResolveAlerts resolves alerts **this integration raised**, recording why.",
        "description": "Scoping matters: an alert of the same type raised by Zentail itself, or by\nanother integration, on the same order survives. Resolving by type alone\nwould let one caller silently clear another's alerts.",
        "operationId": "FulfillmentIntegrationService_ResolveAlerts",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiResolveAlertsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiResolveAlertsRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/fulfillment_orders/confirm_cancellations": {
      "post": {
        "summary": "ConfirmCancellations reports that you have pulled work back on your side,\ndraining it from ListNeedToCancel.",
        "description": "Confirm only what you actually cancelled. If a unit has already shipped and\ncannot be recalled, say so with `already_shipped` rather than confirming —\nZentail needs to know the difference to keep the customer order right.",
        "operationId": "FulfillmentIntegrationService_ConfirmCancellations",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiConfirmCancellationsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiConfirmCancellationsRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/fulfillment_orders/reject": {
      "post": {
        "summary": "RejectFulfillmentOrders tells Zentail you cannot fulfil work it asked for —\nout of stock, damaged, address undeliverable. This is the other direction\nfrom ConfirmCancellations: there, Zentail asked; here, you are declining.",
        "description": "Zentail reroutes the work or surfaces it to the user, and it leaves\nListNeedToShip.",
        "operationId": "FulfillmentIntegrationService_RejectFulfillmentOrders",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiRejectFulfillmentOrdersResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiRejectFulfillmentOrdersRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/need_to_cancel": {
      "get": {
        "summary": "ListNeedToCancel returns fulfillment orders you have acknowledged that\nZentail now wants pulled back — re-routed to another warehouse, or\ncancelled by the buyer or the channel.",
        "description": "Only acknowledged work appears here. If you never told Zentail you had\ncreated it, there is nothing on your side to undo and the units simply\nleave ListNeedToShip.\n\nEvery entry is a total withdrawal: each line cancels at its full routed\nquantity, and nothing is left on this fulfillment order for\nListNeedToShip to show. A merchant's partial reduction is not served\nhere yet — it stays pending until per-line cancellation ships\n(sales-orders, ZEN-3944). Measured against sales-orders on 2026-09-21.",
        "operationId": "FulfillmentIntegrationService_ListNeedToCancel",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiListNeedToCancelResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "cursor",
            "description": "Leave empty for the first page; pass next_cursor thereafter.",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "pageSize",
            "description": "Server-capped. Omit for the default.",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/need_to_ship": {
      "get": {
        "summary": "ListNeedToShip returns fulfillment orders you owe a shipment on: work\nrouted to your warehouses and not yet shipped or cancelled.",
        "description": "New work and re-routed work look the same here, which is the point. A\npartial re-route reduces a line's quantity rather than removing the\nfulfillment order, so `lines` is authoritative on every read and must not\nbe cached across polls.\n\nAn order leaves this queue when its lines are shipped, when Zentail\ncancels it, or when you reject it. Acknowledging does not remove it — you\nstill owe the shipment.",
        "operationId": "FulfillmentIntegrationService_ListNeedToShip",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiListNeedToShipResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "cursor",
            "description": "Leave empty for the first page; pass next_cursor thereafter.",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "pageSize",
            "description": "Server-capped. Omit for the default.",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/shipments": {
      "post": {
        "summary": "ConfirmShipments reports packages that have shipped, draining the shipped\nquantities from ListNeedToShip.",
        "description": "Idempotent on external_shipment_id: replaying a shipment is a no-op, so a\nretry after a timeout can never double-ship.",
        "operationId": "FulfillmentIntegrationService_ConfirmShipments",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiConfirmShipmentsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/shipping_apiConfirmShipmentsRequest"
            }
          }
        ],
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/shipping/status": {
      "get": {
        "summary": "IntegrationStatus returns diagnostic checks for the caller's integration —\nwhether warehouses are bound, whether either queue is being drained, and\nwhether anything has been sitting in one for too long.",
        "description": "If the integration implements WarehouseService, Zentail also asks it about\neach warehouse and folds those checks in, so one call answers \"is this\nworking\" from both sides.",
        "operationId": "FulfillmentIntegrationService_IntegrationStatus",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiIntegrationStatusResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Fulfillment Integration Service"
        ]
      }
    },
    "/v2/warehouse/shipping_status/{warehouseUniqueId}": {
      "get": {
        "summary": "WarehouseStatus returns the integration's own diagnostic checks for one\nwarehouse — the things only it can see, such as expiring credentials, a\ncarrier account problem, or a location it can no longer reach.",
        "description": "Zentail calls this while serving IntegrationStatus, so answer from state\nyou already hold and return quickly. Report a problem as a failing Check\nrather than as a gRPC error: an error is indistinguishable from the\nintegration being unreachable, and loses whatever the check would have\nsaid. Returning no checks means \"nothing to report\", which reads as\nhealthy.",
        "operationId": "WarehouseService_WarehouseStatus",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/shipping_apiWarehouseStatusResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "warehouseUniqueId",
            "description": "The warehouse being asked about, named with the integration's own\nidentifier — the same value that appears on a fulfillment order.",
            "in": "path",
            "required": true,
            "type": "string"
          }
        ],
        "tags": [
          "Warehouse Service"
        ]
      }
    }
  },
  "definitions": {
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    },
    "shipping_apiAcknowledgeFulfillmentOrdersRequest": {
      "type": "object",
      "properties": {
        "acknowledgements": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAcknowledgement"
          },
          "description": "One entry per fulfillment order you have created on your own side.\nRequired and server-capped: an empty list, or one over the cap, fails the\nwhole request rather than answering per entry."
        }
      }
    },
    "shipping_apiAcknowledgeFulfillmentOrdersResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAcknowledgementResult"
          },
          "description": "One result per acknowledgement sent. Match on fulfillment_order_id rather\nthan on position, and read every entry: one failing does not fail the rest."
        }
      }
    },
    "shipping_apiAcknowledgement": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The work you are claiming, as ListNeedToShip gave it. Acknowledging does\nnot drain it from that queue — you still owe the shipment."
        },
        "externalOrderId": {
          "type": "string",
          "description": "Your own identifier for the work. Unique per integration — Zentail rejects\na duplicate rather than recording it twice, which is what makes a retry\nsafe."
        }
      }
    },
    "shipping_apiAcknowledgementResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The acknowledgement this answers, echoed back."
        },
        "success": {
          "type": "boolean",
          "description": "True when Zentail holds your external_order_id against this fulfillment\norder — including when it already did, which reports success with\nalready_acknowledged set. False is always a real failure."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success. This\ncontract carries no error code, so the string is all there is: log it, and\ndo not branch on its wording."
        },
        "alreadyAcknowledged": {
          "type": "boolean",
          "description": "True when this external_order_id was already recorded — a replay, not a\nconflict. Treat as success."
        }
      }
    },
    "shipping_apiAddress": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Who the package is addressed to. Not always the buyer — see\nFulfillmentOrder.buyer_name."
        },
        "company": {
          "type": "string",
          "description": "Business name at the delivery address, when the channel captured one."
        },
        "line1": {
          "type": "string",
          "description": "Street address. The line that is present whenever there is an address at\nall."
        },
        "line2": {
          "type": "string",
          "description": "Apartment, suite, unit or similar. Commonly empty."
        },
        "city": {
          "type": "string",
          "description": "Town or city, as supplied. Free text, and empty when the channel sent none."
        },
        "region": {
          "type": "string",
          "description": "State, province or county, as supplied. Sometimes a code and sometimes a\nfull name, because it is whatever the channel sent — do not key on it."
        },
        "postalCode": {
          "type": "string",
          "description": "ZIP or postal code, as supplied. Not validated, and not checked against the\ncountry."
        },
        "country": {
          "type": "string",
          "description": "Country, as supplied. Usually a two-letter ISO code but not guaranteed to\nbe one, so anything parsing it needs a fallback."
        },
        "phone": {
          "type": "string",
          "description": "Contact number for the carrier, when the channel captured one. Not\nnormalised to E.164 and may carry extensions or punctuation."
        },
        "email": {
          "type": "string",
          "description": "The buyer's email address, for carrier notifications about this delivery.\nIt hangs off the address rather than the order because the two are PII\nunder one retention policy: a redacted order carries neither."
        }
      },
      "description": "Address is the buyer's shipping address exactly as the sales channel supplied\nit. Zentail copies it through without normalising, validating or completing\nany part of it, so treat every field as free text — in particular country and\nregion are **not** guaranteed to be ISO codes.\n\nValidate it against your own carrier's rules and reject with\nUNDELIVERABLE_ADDRESS rather than shipping to a guess. An address that was\nnever captured, or that has been redacted under the PII retention policy, is\nabsent from the fulfillment order entirely rather than arriving half-filled."
    },
    "shipping_apiAlert": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The fulfillment order the problem is on. An alert is always about one piece\nof work; there is no integration-wide alert — IntegrationStatus is where\nthat belongs."
        },
        "type": {
          "$ref": "#/definitions/shipping_apiAlertType",
          "description": "What kind of problem it is. Required, and it is half the identity of the\nalert: Zentail refuses a second open alert of the same type on the same\norder, so the type is what makes a poller's re-raise a no-op."
        },
        "message": {
          "type": "string",
          "description": "Shown to the user. Say what is wrong and what would fix it."
        },
        "lineItemId": {
          "type": "string",
          "description": "Optional: scope the alert to one line."
        }
      }
    },
    "shipping_apiAlertResolution": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The fulfillment order carrying the alert."
        },
        "type": {
          "$ref": "#/definitions/shipping_apiAlertType",
          "description": "Which alert to clear. Only an alert this integration raised is in scope, so\nan alert of the same type raised by Zentail or by another integration\nsurvives."
        },
        "resolution": {
          "type": "string",
          "description": "Why it is resolved. Stored alongside the alert and shown to the user."
        }
      }
    },
    "shipping_apiAlertResolveResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The fulfillment order the alert was on, echoed back."
        },
        "type": {
          "$ref": "#/definitions/shipping_apiAlertType",
          "description": "The alert type, echoed back. Match a result on this and\nfulfillment_order_id together."
        },
        "success": {
          "type": "boolean",
          "description": "True when no alert of this type is open any more, whether this call closed\nit or found nothing to close. False is a real failure."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success."
        },
        "alreadyResolved": {
          "type": "boolean",
          "description": "True when no open alert of this type remained, so nothing changed. Treat as\nsuccess: it is what a retry after a timeout sees, and what a poller sees\nwhen a user resolved the alert by hand first."
        }
      },
      "description": "AlertResolveResult mirrors AlertResult but carries the resolve path's own\nsoft-success flag. AlertResult's already_open only makes sense when raising;\non a retried resolve it would be nonsense, leaving a caller that timed out\nand retried with no honest way to read success."
    },
    "shipping_apiAlertResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The fulfillment order the alert was raised against, echoed back."
        },
        "type": {
          "$ref": "#/definitions/shipping_apiAlertType",
          "description": "The alert type, echoed back. Match a result on the pair of this and\nfulfillment_order_id — together they identify the alert."
        },
        "success": {
          "type": "boolean",
          "description": "True when an open alert of this type exists on the order, whether this call\ncreated it or found it already there. False is a real failure."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success. A missing\n`message` and a type an integration may not raise both land here."
        },
        "alreadyOpen": {
          "type": "boolean",
          "description": "True when an open alert of this type already existed, so nothing was\ncreated. Treat as success — this is the expected steady state for a poller\nre-raising a condition that has not gone away."
        }
      }
    },
    "shipping_apiAlertType": {
      "type": "string",
      "enum": [
        "ALERT_TYPE_UNSPECIFIED",
        "ALERT_TYPE_FULFILL",
        "ALERT_TYPE_LATE_SHIPMENT",
        "ALERT_TYPE_LOST",
        "ALERT_TYPE_OTHER"
      ],
      "default": "ALERT_TYPE_UNSPECIFIED",
      "description": "AlertType maps to Zentail's customer-order alert types. Only the types a\nfulfillment integration can legitimately raise are exposed.\n\n - ALERT_TYPE_UNSPECIFIED: Refused. Every alert names a type.\n - ALERT_TYPE_FULFILL: Something is blocking fulfilment but you have not given up on it. If you\nhave, reject instead.\n - ALERT_TYPE_LATE_SHIPMENT: Will miss, or has missed, the ship-by deadline.\n - ALERT_TYPE_LOST: Shipped but the package is lost in transit.\n - ALERT_TYPE_OTHER: Anything else worth telling a user about. Put the specifics in `message` —\nit is the only thing that reaches a person."
    },
    "shipping_apiCancellationConfirmation": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "Which cancellation request you are answering, from ListNeedToCancel."
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiShipmentLine"
          },
          "description": "Not supported yet. Sending any line fails the entry rather than being\nwidened to the whole request, because treating a partial confirmation as a\nfull one would cancel units you did not. Omit it to confirm the whole\nrequest, which is the only shape ListNeedToCancel asks for today."
        },
        "alreadyShipped": {
          "type": "boolean",
          "description": "Set when you could not cancel because the units are already on their way.\nZentail keeps the customer order truthful rather than showing a\ncancellation that did not happen; report the shipment through\nConfirmShipments as normal."
        }
      }
    },
    "shipping_apiCancellationLine": {
      "type": "object",
      "properties": {
        "lineItemId": {
          "type": "string",
          "description": "Echoes the fulfillment order's line this cancels."
        },
        "sku": {
          "type": "string",
          "description": "The SKU on that line, so a warehouse operator can read the instruction.\nline_item_id is what identifies the line — one SKU can appear on two."
        },
        "quantity": {
          "type": "integer",
          "format": "int32",
          "description": "How many units to pull back — always the line's full routed quantity,\nnever a lesser amount (sales-orders, ZEN-3944 tracks partial support)."
        }
      },
      "description": "CancellationLine is deliberately not a FulfillmentOrderLine. On a\nfulfillment order, quantity means \"routed to your warehouse\"; on a cancel it\nmeans \"pull this many back\", and shipped_quantity / cancelled_quantity have\nno meaning on an instruction at all. An integration that reuses one line\nparser across both would read the wrong number."
    },
    "shipping_apiCancellationReason": {
      "type": "string",
      "enum": [
        "CANCELLATION_REASON_UNSPECIFIED",
        "CANCELLATION_REASON_REROUTED",
        "CANCELLATION_REASON_BUYER_CANCELLED",
        "CANCELLATION_REASON_CHANNEL_CANCELLED",
        "CANCELLATION_REASON_MERCHANT_CANCELLED"
      ],
      "default": "CANCELLATION_REASON_UNSPECIFIED",
      "description": " - CANCELLATION_REASON_UNSPECIFIED: Zentail always sets a reason. Read this as one it could not classify, not\nas an absent cancellation.\n - CANCELLATION_REASON_REROUTED: Re-routed to a different warehouse.\n - CANCELLATION_REASON_BUYER_CANCELLED: Cancelled by the buyer.\n - CANCELLATION_REASON_CHANNEL_CANCELLED: Cancelled by the sales channel.\n - CANCELLATION_REASON_MERCHANT_CANCELLED: Cancelled by a Zentail user."
    },
    "shipping_apiCancellationRequest": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The work being pulled back, the same id ListNeedToShip gave you. This is\nthe only identifier ConfirmCancellations accepts."
        },
        "externalOrderId": {
          "type": "string",
          "description": "Your identifier, from the acknowledgement — the handle you need to find it\non your own side."
        },
        "orderNumber": {
          "type": "string",
          "description": "The customer order this came from, for display and correlation. Not an\nidentifier: one customer order can produce two fulfillment orders for you."
        },
        "warehouseUniqueId": {
          "type": "string",
          "description": "The warehouse the work was routed to, in your own namespace. Carried so a\nmulti-warehouse integration can route the pull-back without re-reading the\nfulfillment order."
        },
        "reason": {
          "$ref": "#/definitions/shipping_apiCancellationReason",
          "description": "Why, so it can be shown to a warehouse operator."
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCancellationLine"
          },
          "description": "The quantities to pull back — always every line at its full routed\nquantity. ListNeedToCancel admits only a total withdrawal from a\nwarehouse today, never a partial one (sales-orders, ZEN-3944 tracks\nthe gap)."
        },
        "requestedTs": {
          "type": "string",
          "format": "date-time",
          "description": "When Zentail asked for the work back. For ageing a queue you have not\ndrained — it is not a deadline, and not an idempotency key."
        }
      },
      "description": "CancellationRequest is Zentail asking for work back."
    },
    "shipping_apiCancellationResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The confirmation this answers, echoed back."
        },
        "success": {
          "type": "boolean",
          "description": "True when the request is drained, which includes a replay (already_recorded\nset) and a report of already_shipped — you answered the question you were\nasked. False is a real failure, and the request will be re-offered."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success. Two\nfailures are worth handling rather than retrying: no outstanding request\nfor this order, and an order that has already shipped or been rejected."
        },
        "alreadyRecorded": {
          "type": "boolean",
          "description": "True when this cancellation was already recorded. Treat as success."
        }
      }
    },
    "shipping_apiCheck": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Stable identifier, not prose — an operator or an alert matches on this, so\nit must not change when the wording does. Lower_snake_case by convention."
        },
        "state": {
          "$ref": "#/definitions/shipping_apiCheckState",
          "description": "How bad this is. FAIL means the thing the check covers is broken now; WARN\nmeans it still works but is degrading, which is where a queue that has\nstopped being drained shows up first."
        },
        "message": {
          "type": "string",
          "description": "Prose for a human. Say what is wrong and what would fix it."
        },
        "source": {
          "$ref": "#/definitions/shipping_apiCheckSource",
          "description": "Who observed this. Zentail sets it; an integration filling it in on a\nWarehouseStatus response has it overwritten with INTEGRATION."
        },
        "warehouseUniqueId": {
          "type": "string",
          "description": "Which warehouse this check is about, when it is about one. Set by Zentail\nas it folds a WarehouseStatus response in, and on a check reporting that a\nwarehouse could not be reached.\n\nWithout it the fold is lossy: two warehouses returning the same check name\nare indistinguishable, and anything matching on name alone cannot say which\none is broken. Empty on checks about the integration as a whole."
        }
      }
    },
    "shipping_apiCheckSource": {
      "type": "string",
      "enum": [
        "CHECK_SOURCE_UNSPECIFIED",
        "CHECK_SOURCE_ZENTAIL",
        "CHECK_SOURCE_INTEGRATION"
      ],
      "default": "CHECK_SOURCE_UNSPECIFIED",
      "description": " - CHECK_SOURCE_UNSPECIFIED: Not set. Zentail fills this in on every check it returns, so this value\nonly appears on a check an integration sent and Zentail has not folded in.\n - CHECK_SOURCE_ZENTAIL: Zentail observed this from the outside.\n - CHECK_SOURCE_INTEGRATION: The integration reported this about itself."
    },
    "shipping_apiCheckState": {
      "type": "string",
      "enum": [
        "CHECK_STATE_UNSPECIFIED",
        "CHECK_STATE_PASS",
        "CHECK_STATE_WARN",
        "CHECK_STATE_FAIL"
      ],
      "default": "CHECK_STATE_UNSPECIFIED",
      "description": " - CHECK_STATE_UNSPECIFIED: Zentail never sends this. Treat it as a check you cannot interpret rather\nthan as a pass.\n - CHECK_STATE_PASS: Healthy, nothing to do.\n - CHECK_STATE_WARN: Working, but heading somewhere bad — a queue draining slower than it fills,\na credential close to expiring. Worth looking at before it becomes a FAIL.\n - CHECK_STATE_FAIL: Broken now. Orders this check covers are not moving until it clears."
    },
    "shipping_apiConfirmCancellationsRequest": {
      "type": "object",
      "properties": {
        "confirmations": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCancellationConfirmation"
          },
          "description": "One entry per cancellation request you have carried out. Required and\nserver-capped, like every batch on this contract."
        }
      }
    },
    "shipping_apiConfirmCancellationsResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCancellationResult"
          },
          "description": "One result per confirmation sent, matched on fulfillment_order_id. An entry\nthat failed is still in ListNeedToCancel on the next poll."
        }
      }
    },
    "shipping_apiConfirmShipmentsRequest": {
      "type": "object",
      "properties": {
        "shipments": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiShipment"
          },
          "description": "One entry per package. Required and server-capped: an empty list, or one\nover the cap, fails the whole request rather than answering per entry."
        }
      }
    },
    "shipping_apiConfirmShipmentsResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiShipmentResult"
          },
          "description": "One result per shipment sent. Match on external_shipment_id rather than on\nposition, and read every entry — one package failing does not fail the\nothers, and the units it covered are still owed."
        }
      }
    },
    "shipping_apiFulfillmentOrder": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "Zentail's identifier for this unit of work, and the handle for every write\nin this API. Stable for the life of the fulfillment order."
        },
        "orderNumber": {
          "type": "string",
          "description": "The customer order this came from. For display and correlation only — it is\nnot unique to you, and is not accepted as an identifier on any write."
        },
        "externalOrderId": {
          "type": "string",
          "description": "Set once you have acknowledged it."
        },
        "status": {
          "$ref": "#/definitions/shipping_apiFulfillmentOrderStatus",
          "description": "Where the work has got to. Informational only, and possibly a poll behind\nthe queues — see the enum for why nothing should branch on it."
        },
        "warehouseUniqueId": {
          "type": "string",
          "description": "The warehouse these lines are routed to, in your own namespace."
        },
        "orderedTs": {
          "type": "string",
          "format": "date-time",
          "description": "When the buyer placed the customer order — not when the work reached you.\nAge your own queue on assigned_ts instead; an order can be routed to a\nwarehouse days after it was placed."
        },
        "assignedTs": {
          "type": "string",
          "format": "date-time",
          "description": "When this work was routed to your warehouse."
        },
        "lastUpdatedTs": {
          "type": "string",
          "format": "date-time",
          "description": "When Zentail last changed anything here, a re-route included. It tells you\na cached copy is stale; it does not say what changed, so re-read `lines`\nrather than diffing on it."
        },
        "shipByTs": {
          "type": "string",
          "format": "date-time",
          "description": "Deadline for handing the package to the carrier. Not populated: Zentail has\nno ship-by column to read, and will not approximate one from another\ntimestamp because a fabricated deadline is worse than none to a fulfiller\nthat prioritises on it. Always unset today — measured against sales-orders\non 2026-09-21. Prioritise on assigned_ts."
        },
        "shipTo": {
          "$ref": "#/definitions/shipping_apiAddress",
          "description": "Where the package goes. Unset when the order has no usable shipping address\n— none was captured, or it was redacted under the PII retention policy — and\nin that case there is nothing to ship to. Raise an alert or reject with\nUNDELIVERABLE_ADDRESS rather than guessing one."
        },
        "requestedServiceLevel": {
          "type": "string",
          "description": "The shipping speed the buyer bought, as Zentail's standardised service\nlevel for the order. Empty when the channel named none. Map it onto one of\nyour own carrier services; it does not name a carrier."
        },
        "buyerName": {
          "type": "string",
          "description": "The buyer, for packing slips and support. Not the addressee:\nship_to.name is who the label goes to and legitimately differs on a gift or\na business delivery. Address the package from ship_to."
        },
        "giftMessage": {
          "type": "string",
          "description": "Not populated. Zentail has no gift-message column to read one from, so this\nis always empty and is not evidence that the order carries no gift message.\nMeasured against sales-orders on 2026-09-21."
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiFulfillmentOrderLine"
          },
          "description": "Authoritative on every read. Quantities change when Zentail re-routes work\nin or out of this warehouse, so never cache them across polls."
        },
        "openAlerts": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAlert"
          },
          "description": "Alerts you currently have open on this fulfillment order. Returned so a\npoller can see what it has already raised without keeping its own record —\nthe same reason Zentail holds external_order_id."
        },
        "shippingPrice": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "What the buyer paid to have the order shipped, and the tax on it. Order\nlevel, not per line: the buyer pays it once however the order splits, so a\nfulfillment order covering part of an order carries the whole figure and it\nmust not be summed across siblings."
        },
        "shippingTax": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "Tax charged on that shipping price. Order level on the same terms, so it\nmust not be summed across the fulfillment orders of one customer order."
        },
        "discount": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "Discount applied to the order as a whole."
        },
        "totalPayment": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "What the buyer actually paid, in total, on the channel they bought from.\n\nDeliberately separate from the lines rather than derived from them. It is\nthe transaction, and it can legitimately disagree with the composed total:\nan order part-refunded, cancelled, or not yet paid. Measured across ~37k\nreal orders it matches the composition about 89% of the time, and the\nremainder is mostly lifecycle. per ZEN-4048.\n\nSo record payment from this, and compose the order from the lines. Do not\nreconcile one into the other by inventing an adjustment line."
        }
      },
      "description": "FulfillmentOrder is a lean view of the work: only the lines routed to one of\nthe caller's warehouses, with the quantities routed there."
    },
    "shipping_apiFulfillmentOrderLine": {
      "type": "object",
      "properties": {
        "lineItemId": {
          "type": "string",
          "description": "Stable per line; echo it back on shipment, cancellation and rejection."
        },
        "sku": {
          "type": "string",
          "description": "Zentail's SKU for the unit to pick. Report work by line_item_id rather than\nby this: one SKU can appear on two lines of the same order."
        },
        "title": {
          "type": "string",
          "description": "The product name, for packing slips and pick lists. Display only — it can\nchange between polls and identifies nothing."
        },
        "quantity": {
          "type": "integer",
          "format": "int32",
          "description": "Routed to your warehouse, not the customer order's total."
        },
        "shippedQuantity": {
          "type": "integer",
          "format": "int32",
          "description": "Always zero, deliberately. `quantity` is already only what you still owe,\nso subtracting this would double-count what has shipped. Your own\nConfirmShipments calls are the record of shipments, not this field.\nMeasured against sales-orders on 2026-09-21."
        },
        "cancelledQuantity": {
          "type": "integer",
          "format": "int32",
          "description": "Always zero, on the same terms as shipped_quantity."
        },
        "unitPrice": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "What the buyer paid per unit, before tax. Not Zentail's cost.\n\nSend this on to a store that prices the line itself, or it falls back to\nits own catalogue price and misstates the order, which is what happened\nbefore this field existed. per ZEN-4048."
        },
        "tax": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "Tax for the whole line, not per unit. Unset when the order is untaxed."
        }
      }
    },
    "shipping_apiFulfillmentOrderStatus": {
      "type": "string",
      "enum": [
        "FULFILLMENT_ORDER_STATUS_UNSPECIFIED",
        "FULFILLMENT_ORDER_STATUS_NEW",
        "FULFILLMENT_ORDER_STATUS_ACCEPTED",
        "FULFILLMENT_ORDER_STATUS_PARTIALLY_SHIPPED",
        "FULFILLMENT_ORDER_STATUS_SHIPPED",
        "FULFILLMENT_ORDER_STATUS_CANCELLED",
        "FULFILLMENT_ORDER_STATUS_PARTIALLY_CANCELLED",
        "FULFILLMENT_ORDER_STATUS_REJECTED"
      ],
      "default": "FULFILLMENT_ORDER_STATUS_UNSPECIFIED",
      "description": "Status is informational. Do not drive behaviour from it — the queues already\nsay what Zentail wants done, and a status left over from a previous poll is\nhow an integration ends up shipping work it no longer owes.\n\n - FULFILLMENT_ORDER_STATUS_UNSPECIFIED: Not a state Zentail sends. Read it as a status you cannot interpret.\n - FULFILLMENT_ORDER_STATUS_NEW: Routed to you, not yet acknowledged.\n - FULFILLMENT_ORDER_STATUS_ACCEPTED: Acknowledged and awaiting shipment.\n - FULFILLMENT_ORDER_STATUS_PARTIALLY_SHIPPED: Some units shipped and some are still owed; the remainder stays in\nListNeedToShip.\n - FULFILLMENT_ORDER_STATUS_SHIPPED: Every routed unit shipped. Nothing further is owed.\n - FULFILLMENT_ORDER_STATUS_CANCELLED: Zentail pulled the whole thing back.\n - FULFILLMENT_ORDER_STATUS_PARTIALLY_CANCELLED: Part was pulled back. Ship whatever ListNeedToShip still shows.\n - FULFILLMENT_ORDER_STATUS_REJECTED: You declined it. Zentail reroutes or surfaces it."
    },
    "shipping_apiIntegrationStatusResponse": {
      "type": "object",
      "properties": {
        "checks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCheck"
          },
          "description": "Zentail's own observations and the integration's last reported checks, in\none list. Read `source` to tell them apart; render them together, because\nan operator asking \"is this working\" does not care who noticed."
        }
      }
    },
    "shipping_apiListNeedToCancelResponse": {
      "type": "object",
      "properties": {
        "cancellations": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCancellationRequest"
          },
          "description": "What Zentail wants pulled back, one fulfillment order per entry. Only work\nyou acknowledged appears here. Each entry is re-offered on every poll until\nyou confirm it, so an entry you have already handled means the\nconfirmation did not land."
        },
        "nextCursor": {
          "type": "string",
          "description": "Empty when the page is the last one."
        }
      }
    },
    "shipping_apiListNeedToShipResponse": {
      "type": "object",
      "properties": {
        "orders": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiFulfillmentOrder"
          },
          "description": "The work you owe a shipment on, one fulfillment order per entry. Every read\nis authoritative rather than incremental: quantities on `lines` are what is\nrouted to you now, and a re-route can lower them between polls."
        },
        "nextCursor": {
          "type": "string",
          "description": "Empty when the page is the last one."
        }
      }
    },
    "shipping_apiMoney": {
      "type": "object",
      "properties": {
        "amount": {
          "type": "string",
          "description": "Decimal string, e.g. \"13.00\". Negative for a credit."
        },
        "currencyCode": {
          "type": "string",
          "description": "ISO 4217, e.g. \"USD\"."
        }
      },
      "description": "Money is an amount and the currency it is in.\n\nThe amount is a decimal string, not a float or units+nanos. Money in this\ncontract crosses into Shopify's MoneyBagInput, which is itself a decimal\nstring, so a string maps across untouched — no scaling and no rounding at the\nedge, which is where money bugs come from. Follows the Money already in\napi-proto's listing contract rather than google.type.Money, for that reason.\n\ncurrency_code travels with every amount rather than sitting once on the\norder. Multi-currency is real (etp has\npurchase_orders.non_standard_currencies), and a silently wrong currency is\nworse than a wrong amount: the number still looks plausible."
    },
    "shipping_apiRaiseAlertsRequest": {
      "type": "object",
      "properties": {
        "alerts": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAlert"
          },
          "description": "One entry per condition to raise. Required and server-capped, like every\nbatch on this contract. Safe to re-send the same alerts every poll: a\nsecond open alert of the same type on the same order is not created."
        }
      }
    },
    "shipping_apiRaiseAlertsResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAlertResult"
          },
          "description": "One result per alert sent, keyed by fulfillment order and type. Read\nalready_open before reacting to anything: on a steady-state poller most\nentries are re-raises of a condition that has not gone away."
        }
      }
    },
    "shipping_apiRejectFulfillmentOrdersRequest": {
      "type": "object",
      "properties": {
        "rejections": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiRejection"
          },
          "description": "One entry per fulfillment order you are declining. Required and\nserver-capped, like every batch on this contract."
        }
      }
    },
    "shipping_apiRejectFulfillmentOrdersResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiRejectionResult"
          },
          "description": "One result per rejection sent, matched on fulfillment_order_id. Anything\nthat failed is still in ListNeedToShip on the next poll."
        }
      }
    },
    "shipping_apiRejection": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The work you are handing back, from ListNeedToShip."
        },
        "reason": {
          "$ref": "#/definitions/shipping_apiRejectionReason",
          "description": "Why you cannot fulfil it. Required — an unset reason is refused rather than\nstored, since the reason is the whole content of the record and OTHER\nalready covers anything outside the list."
        },
        "detail": {
          "type": "string",
          "description": "Free text shown to the user alongside the reason."
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiShipmentLine"
          },
          "description": "Not supported yet. Sending any line fails the entry rather than declining\nthe whole order, which would hand back units you can still ship, silently.\nOmit it to decline the whole fulfillment order."
        }
      }
    },
    "shipping_apiRejectionReason": {
      "type": "string",
      "enum": [
        "REJECTION_REASON_UNSPECIFIED",
        "REJECTION_REASON_OUT_OF_STOCK",
        "REJECTION_REASON_DAMAGED",
        "REJECTION_REASON_UNDELIVERABLE_ADDRESS",
        "REJECTION_REASON_SKU_NOT_FOUND",
        "REJECTION_REASON_OTHER"
      ],
      "default": "REJECTION_REASON_UNSPECIFIED",
      "description": "RejectionReason is shown to the Zentail user deciding what to do next, so it\nis required on every rejection. Pick the one that describes your side of the\nproblem; OTHER plus Rejection.detail is better than a near-miss.\n\n - REJECTION_REASON_UNSPECIFIED: Refused. Send a real reason, or OTHER.\n - REJECTION_REASON_OUT_OF_STOCK: You hold the SKU but not enough of it to ship this work.\n - REJECTION_REASON_DAMAGED: The units are there but not shippable — damaged, expired, or failed a\nquality check.\n - REJECTION_REASON_UNDELIVERABLE_ADDRESS: The address will not deliver: your carrier refuses it, or it is incomplete.\nThe likeliest reason a fulfillment order arrives with no ship_to at all.\n - REJECTION_REASON_SKU_NOT_FOUND: The SKU is not one you stock at all. Distinct from OUT_OF_STOCK: that one\nclears when stock arrives, this one needs someone to fix the catalogue or\nthe routing.\n - REJECTION_REASON_OTHER: Anything else. Put the specifics in Rejection.detail — a user reads both."
    },
    "shipping_apiRejectionResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The rejection this answers, echoed back."
        },
        "success": {
          "type": "boolean",
          "description": "True when the work is out of your queue, which includes a repeat of a\nrejection already recorded. False is a real failure and the work is still\nyours to ship or decline."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success. The one\nworth handling rather than retrying is an order that has shipped,\npart-shipped or been cancelled, which can no longer be declined."
        }
      }
    },
    "shipping_apiResolveAlertsRequest": {
      "type": "object",
      "properties": {
        "resolutions": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAlertResolution"
          },
          "description": "One entry per alert to clear. Required and server-capped. Only alerts this\nintegration raised are in scope — naming one raised by Zentail or by\nanother integration clears nothing."
        }
      }
    },
    "shipping_apiResolveAlertsResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiAlertResolveResult"
          },
          "description": "One result per resolution sent, keyed by fulfillment order and type. Read\nalready_resolved before reacting: nothing open to clear is the normal\nanswer to a retry, and to a user having cleared it first."
        }
      }
    },
    "shipping_apiShipment": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "Which fulfillment order this package is against. One package cannot span\ntwo fulfillment orders, so a consignment covering both halves of a split\norder is two Shipments."
        },
        "externalShipmentId": {
          "type": "string",
          "description": "Your identifier for this package. Idempotency key: a repeat is a no-op."
        },
        "carrier": {
          "type": "string",
          "description": "Who is carrying it, as you name them — free text, not a Zentail enum. Shown\nto the buyer and to support alongside the tracking number."
        },
        "trackingNumber": {
          "type": "string",
          "description": "Required. A package sent without one is refused rather than recorded,\nbecause Zentail drops an untracked package silently: accepting it would\nreport success for a shipment that was never stored, leaving the units owed\nwith nothing saying why."
        },
        "trackingUrl": {
          "type": "string",
          "description": "Not currently stored. Zentail tracks by carrier and tracking number and has\nnowhere to put a per-package URL, so anything sent here is accepted and\ndiscarded. Measured against sales-orders on 2026-09-21."
        },
        "serviceLevel": {
          "type": "string",
          "description": "Required, and not defaulted. The shipping speed you actually used, in your\nown vocabulary — Zentail will not substitute a value, because one guess for\nevery fulfiller hides which of them reported nothing. Send a placeholder of\nyour own if you have no real level to report."
        },
        "shippedTs": {
          "type": "string",
          "format": "date-time",
          "description": "When the package was handed to the carrier. Omit it and Zentail stamps the\ntime the request arrived, which is the right behaviour for a confirmation\nsent immediately; send it when you are catching up on a backlog."
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiShipmentLine"
          },
          "description": "What is in the package. Omit to ship everything the fulfillment order still\nowes — the common case. Naming lines ships those quantities only; a\nline_item_id not on the order, a quantity below one, or quantities summing\npast what is owed fails this package and leaves the units owed."
        },
        "shippingCost": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "What it cost the fulfiller to ship this package. Unset when they do not\nreport one; unset means unknown and must not be read as zero, which would\nsilently inflate margin. per ZEN-4054."
        },
        "handlingCost": {
          "$ref": "#/definitions/shipping_apiMoney",
          "description": "A pick, pack or handling fee the fulfiller charges for this package, kept\nseparate from postage rather than folded into it. Folding them loses the\ndistinction permanently: postage is the carrier's price and varies by\ndestination and weight, while a handling fee is the fulfiller's own and is\nusually flat, so a margin question about one cannot be answered from their\nsum. Unset means unknown, on the same terms as shipping_cost. per ZEN-4054."
        }
      }
    },
    "shipping_apiShipmentLine": {
      "type": "object",
      "properties": {
        "lineItemId": {
          "type": "string",
          "description": "Which line of the fulfillment order, echoed from FulfillmentOrderLine. A\nSKU is not accepted here: one SKU can appear on two lines."
        },
        "quantity": {
          "type": "integer",
          "format": "int32",
          "description": "How many units of that line are in this package. At least one, and never\nmore than the line still owes — across the whole request, so naming one\nline twice is summed before it is checked."
        }
      }
    },
    "shipping_apiShipmentResult": {
      "type": "object",
      "properties": {
        "fulfillmentOrderId": {
          "type": "string",
          "description": "The fulfillment order this package was against, echoed back."
        },
        "externalShipmentId": {
          "type": "string",
          "description": "Your own id for the package, echoed back. This is what to match a result\non: one call may carry several packages for the same fulfillment order."
        },
        "success": {
          "type": "boolean",
          "description": "True when the package is recorded, including when it already was — a replay\nreports success with already_recorded set. False is always a real failure,\nand the units it covered are still owed."
        },
        "errorMessage": {
          "type": "string",
          "description": "Why it failed, in prose, for logs and support. Empty on success. No error\ncode accompanies it, so log the string rather than branching on it."
        },
        "alreadyRecorded": {
          "type": "boolean",
          "description": "True when this shipment was already recorded. Treat as success."
        }
      }
    },
    "shipping_apiWarehouseStatusResponse": {
      "type": "object",
      "properties": {
        "checks": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/shipping_apiCheck"
          },
          "description": "Everything the integration knows about this warehouse's health, one entry\nper condition. Leave `source` and `warehouse_unique_id` unset; Zentail\noverwrites both as it folds these into IntegrationStatus. An empty list\nmeans nothing to report, which Zentail renders as healthy."
        }
      }
    }
  }
}
